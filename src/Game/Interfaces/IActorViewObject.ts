import { EvalSourceMapDevToolPlugin } from "webpack";

interface IActorViewObject {
  spriteXOffset: number,
  spriteYOffset: number,
  spriteWidth: number,
  spriteHeight: number,
  xpos: number,
  ypos: number,
  imageWidth: number,
  imageHeight: number,
}

export default IActorViewObject;
